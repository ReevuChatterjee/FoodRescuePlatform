"""Person 5 — routing engine: distance, ETA with traffic, route geometry.

Import from app.routing.service (calculate_route, estimate_route,
estimate_routes_from) and app.routing.geo (haversine_km, parse_latlng, ...).
"""
