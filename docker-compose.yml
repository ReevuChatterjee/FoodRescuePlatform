version: '3.9'

services:
  db:
    image: postgis/postgis:16-3.4
    container_name: cpi_db
    environment:
      POSTGRES_USER: cpi_user
      POSTGRES_PASSWORD: cpi_pass
      POSTGRES_DB: cpi_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - cpi_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cpi_user -d cpi_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: cpi_redis
    ports:
      - "6379:6379"
    networks:
      - cpi_net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: cpi_backend
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - cpi_net
    volumes:
      - ./backend:/app
      - ./tests:/tests
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: cpi_worker
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - cpi_net
    volumes:
      - ./backend:/app
    command: celery -A app.worker worker --loglevel=info
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: cpi_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    networks:
      - cpi_net
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - VITE_API_BASE_URL=http://localhost:8000
    command: npm run dev -- --host 0.0.0.0

volumes:
  postgres_data:

networks:
  cpi_net:
    driver: bridge
